// 兼容版测试 - 只使用编译器支持的语法
// 1. 变量定义和基本数学运算
var x = 10;
var y = 20;
var z = x + y;
print(z);

// 2. 字符串操作（只能简单拼接）
var str1 = "Hello";
var str2 = "World";
var str3 = str1 + str2;
print(str3);

// 3. 简单属性访问
var obj = {
    name: "Stack",
    version: 1.0
};

print(obj.name);
print(obj.version);

// 4. 变量重新赋值
z = z + x;
print(z);